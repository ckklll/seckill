实现了共享session登录，生成分布式唯一id，优惠券抢购的秒杀功能，具体流程是用户发送请求->lua脚本原子校验库存是否充足和是否重复下单，校验通过把用户Id保存到Set中，防止重复下单。
校验通过后直接创建订单加入阻塞队列->返回给用户结果。然后开启一个线程异步执行订单写入数据库，订单写入数据库通过Redisson实现分布式锁作为兜底方案，先获取分布式锁，成功后进行库存校验，重复下单校验，写入数据库，最后释放锁，确保不会重复下单和超卖。
